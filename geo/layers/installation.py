"""
Phase 3 stub — installation layer per cell. NULL in MVP.
When implemented: observed military sites stay at cell granularity,
modelled as assessments with confidence+staleness, NEVER authoritative coordinates.
"""
from __future__ import annotations


def load_installation(theater_id: str, conn, *args, **kwargs) -> int:
    return 0
