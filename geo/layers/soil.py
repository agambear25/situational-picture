"""Phase 3 stub — soil type per cell. NULL in MVP; keeps cell_context schema valid."""
from __future__ import annotations


def load_soil(theater_id: str, conn, *args, **kwargs) -> int:
    # Phase 3 only — not downloaded or processed in MVP
    return 0
